extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    if res == []
      each val in res
        li
          a(href='/q/' + val.name)= val.name
    else
      p No queries setup yet
