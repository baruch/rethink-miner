extends layout

block content
  table(class=['table-striped', 'table'])
    thead
      tr
        th= key
        th Count
    tbody
      each key in result
        tr
          td= key.key
          td= key.count
